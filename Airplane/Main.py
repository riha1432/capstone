import ray
import asyncio
import Parallel_Processing as parallel

ray.init(num_cpus = 8)


async def setup():
    pass

async def main():
    pass

if(__name__ == "__main__")
    asyncio.run(setup())
    asyncio.run(main())